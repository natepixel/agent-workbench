# main

This file is an example of a branch status note. It lives here because
`scripts/status.sh` looks up `working/status/<safe-branch>.md` for the current
branch. See [`README.md`](README.md) for the filename convention.

## Date
2026-04-27

## Branch
`main`

## Goal
Keep the template coherent and useful as a reusable starter repo.

## Last meaningful commit(s)
- `Update MIGRATE-REPO prompt with today's conventions`
- `Add agent-memory convention, stop.sh, real CI, and pointer files`

## Current status
Stable baseline. Scripts (`dev.sh` / `stop.sh` / `status.sh`), CI, and the
`working/` ↔ `skills/` ↔ `docs/` promotion model are all in place. Example
skills and docs are starter content meant to be replaced when forked.

## Blockers
None.

## Next step
Either fork the template into a real project, or improve the template itself
on a feature branch. When working on the template, capture per-branch progress
in `working/status/<branch>.md` using this file as a model.
